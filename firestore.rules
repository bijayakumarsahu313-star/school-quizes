
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Public read-only access.
      // Writes are disabled to prevent anonymous users from modifying data.
      allow read: if true;
      allow write: if false;
    }
  }
}
